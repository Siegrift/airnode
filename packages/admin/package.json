{
  "name": "@airnode/admin",
  "license": "MIT",
  "version": "2.1.0",
  "bin": "./cli-wrapper.js",
  "main": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@airnode/airnode-abi": "^2.0.0",
    "@airnode/protocol": "^2.1.0"
  },
  "publishConfig": {
    "registry": "http://localhost:4873"
  }
}
